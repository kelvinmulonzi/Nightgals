HELP.md
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

# Nightgals
var/storage/
# Real credentials. .env.example is the committed template; this never is.
#
# This comment existed without the rule under it, so .env was tracked and
# committed anyway - which is how a Gmail app password, JWT_SECRET,
# DOCUMENT_HASH_PEPPER and the MTN credentials ended up in the history.
#
# The rule itself, which is what was missing. GitHub's push protection caught
# the Stripe secret key on the way out; without this line it would simply be
# recommitted next time.
.env
.env.local
.env.*.local
